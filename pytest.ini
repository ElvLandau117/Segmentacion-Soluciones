[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --tb=short
markers =
    requires_checkpoints: test needs real .pth files in CHECKPOINTS_DIR (skipped in CI)
    slow: test takes > 5s
