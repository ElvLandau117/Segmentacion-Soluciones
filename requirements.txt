torch>=2.2.0
torchvision>=0.17.0
segmentation-models-pytorch>=0.3.3
albumentations>=1.4.0
opencv-python-headless>=4.9.0
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0
scikit-image>=0.22.0
scipy>=1.12.0
matplotlib>=3.8.0
seaborn>=0.13.0
mlflow>=2.10.0
# HF Spaces uses sdk_version from README YAML; this floor mirrors it so local
# dev gets the same gradio-client schema fix (gradio 5.30+ resolves the
# TypeError 'bool not iterable' in gradio_client/utils.py:get_type).
gradio>=5.50.0,<6.0.0
Pillow>=10.2.0
tqdm>=4.66.0
huggingface_hub>=0.33.5,<2.0  # gradio 5.50 requires >=0.33.5; the old <0.28 pin was for gradio 4.44's HfFolder
grad-cam>=1.5.0

# Testing (dev-only; safe to install alongside runtime)
pytest>=7.4.0
